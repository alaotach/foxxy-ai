# Analytics MCP Server Configuration
# Copy to .env.mcp and configure

# Server Configuration
MCP_HOST=0.0.0.0
MCP_PORT=8001

# Authentication
MCP_API_KEY=your_secure_api_key_here_change_this

# SSL Configuration (optional - use nginx/caddy instead for production)
# SSL_KEYFILE=/path/to/ssl/key.pem
# SSL_CERTFILE=/path/to/ssl/cert.pem

# Logging
LOG_LEVEL=INFO
